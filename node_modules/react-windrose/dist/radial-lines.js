import { jsx as _jsx } from "react/jsx-runtime";
import { range } from "d3-array";
/**
 * Renders a set of radial grid lines extending from the inner radius to the last tick
 * @param props - The component props
 * @returns A group of SVG line elements forming radial grid lines
 */
export function RadialLines({ angleStep, innerRadius, yScale, tickCount, ...props }) {
    const x1 = innerRadius;
    const lastYTick = yScale.ticks(tickCount).at(-1);
    const x2 = yScale(lastYTick ?? 0); // to the last tick circle
    return (_jsx("g", { name: "radial-lines", fill: "none", stroke: "gray", strokeWidth: "1", strokeDasharray: "1,4", ...props, children: range(-90, 270, angleStep).map((rotation) => (_jsx("line", { x1: x1, x2: x2, transform: `rotate(${rotation})` }, rotation))) }));
}
//# sourceMappingURL=radial-lines.js.map