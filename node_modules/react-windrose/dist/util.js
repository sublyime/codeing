export function radians(degrees) {
    return (degrees * Math.PI) / 180;
}
export const fluidFontSize = (scale) => (outerRadius) => Math.max(8, Math.min(18, outerRadius * scale));
/** One full turn in degrees */
export const TURN = 360;
// d3.schemeBlues[9] - using darker colors for better visibility
export const blueColorScheme = [
    "#c6dbef",
    "#9ecae1",
    "#6baed6",
    "#4292c6",
    "#2171b5",
    "#08519c",
    "#08306b",
    "#0056b3",
    "#003d82",
];
export function sumRow(row, bins) {
    let sum = 0;
    for (const bin of bins) {
        const value = row[bin];
        if (typeof value === "number") {
            sum += value;
        }
        else if (typeof value === "string") {
            const numValue = Number(value);
            if (!isNaN(numValue)) {
                sum += numValue;
            }
        }
    }
    return sum;
}
//# sourceMappingURL=util.js.map