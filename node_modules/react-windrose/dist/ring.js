import { jsxs as _jsxs, jsx as _jsx } from "react/jsx-runtime";
/**
 * Renders a single ring of a windrose chart
 * @param props - The component props
 * @returns An SVG group element containing the ring
 */
export function Ring({ element, angleOffset, colorScale, arcGenerator, ...props }) {
    return (_jsx("g", { name: `ring-bin-${element.key}`, fill: colorScale(element.key), ...props, children: element.map((point) => {
            const path = arcGenerator(point);
            if (!path) {
                return null;
            }
            return (_jsx("path", { name: `${element.key}_${point.data.direction}`, d: path, transform: `rotate(${angleOffset})`, children: _jsxs("title", { children: [point.data.direction, ": ", element.key] }) }, `${element.key}-${point.data.direction}`));
        }) }));
}
//# sourceMappingURL=ring.js.map